{
	"name": "pods",
	"type": "module",
	"scripts": {
		"clean": "rm -rf dist",
		"build": "tsgo -p tsconfig.build.json && cp src/models.json dist/ && cp -r scripts dist/"
	},
	"dependencies": {
		"agent": "*",
		"chalk": "^5.5.0"
	},
	"devDependencies": {}
}
