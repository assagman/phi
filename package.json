{
	"name": "phi",
	"private": true,
	"type": "module",
	"workspaces": [
		"packages/*",
		"packages/coding-agent/examples/extensions/with-deps"
	],
	"scripts": {
		"clean": "bun run --workspaces clean",
		"build": "bun run --filter tui build && bun run --filter ai build && bun run --filter agent build && bun run --filter coding-agent build && bun run --filter pods build",
		"dev": "concurrently --names \"ai,agent,coding-agent,tui\" --prefix-colors \"cyan,yellow,red,magenta\" \"bun run --filter ai dev\" \"bun run --filter agent dev\" \"bun run --filter coding-agent dev\" \"bun run --filter tui dev\"",
		"dev:tsc": "bun run --filter ai dev:tsc",
		"check": "biome check --write . && tsgo --noEmit",
		"test": "bun test --workspaces",
		"prepare": "husky"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.5",
		"@types/node": "^22.10.5",
		"@typescript/native-preview": "7.0.0-dev.20251212.1",
		"concurrently": "^9.2.1",
		"husky": "^9.1.7",
		"tsx": "^4.20.3",
		"typescript": "^5.9.2"
	},
	"dependencies": {
		"jiti": "^2.6.1",
		"get-east-asian-width": "^1.4.0"
	}
}
